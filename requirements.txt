# ============================================================
# SAGE Crop Disease — LoRA Fine-Tuning
# Requirements for GCP Compute Engine / Vertex AI / Kaggle
# ============================================================

# ----- PyTorch (installed via environment on Kaggle/GCP Deep Learning VMs) -----
# On Kaggle and GCP DL VMs, torch is pre-installed. Uncomment if installing manually:
# torch>=2.1.0
# torchvision>=0.16.0

# ----- HuggingFace Ecosystem -----
transformers>=4.48.0
peft>=0.10.0
accelerate>=0.28.0
datasets>=2.16.0          # required for streaming mode
huggingface-hub>=0.20.0
qwen-vl-utils>=0.0.8

# ----- Data & Image Processing -----
pillow>=10.0.0
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
pyarrow>=14.0.0       # Parquet reading
fastparquet>=2023.10  # Alternative parquet engine
scikit-learn>=1.3.0

# ----- Config & Utilities -----
pyyaml>=6.0
tqdm>=4.65.0
psutil>=5.9.0

# ----- Optional: GCP Cloud Storage sync -----
google-cloud-storage>=2.14.0
