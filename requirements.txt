# ============================================================
# SAGE Crop Disease Visual Diagnosis — Python Requirements
# Optimized for Google Cloud Platform (GCP VM / Vertex AI / Compute Engine)
# ============================================================

# ----- PyTorch & CUDA Ecosystem -----
# For GCP Compute Engine / Vertex AI instances with NVIDIA GPUs (T4, L4, V100, A100)
torch>=2.1.0
torchvision>=0.16.0

# ----- HuggingFace Ecosystem & QLoRA -----
# Qwen2.5-VL vision-language model & 4-bit NF4 quantization dependencies
transformers>=4.48.0
peft>=0.10.0
bitsandbytes>=0.43.0
accelerate>=0.28.0
datasets>=2.16.0
huggingface-hub>=0.20.0
qwen-vl-utils>=0.0.8

# ----- Data Processing, Computer Vision & Math -----
pillow>=10.0.0
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
pyyaml>=6.0
tqdm>=4.65.0

# ----- GCP Cloud Storage & Infrastructure Utilities -----
google-cloud-storage>=2.14.0
psutil>=5.9.0
